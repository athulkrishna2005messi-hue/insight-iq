# Whop Integration Secrets
# These values are for local development and testing only.
# In production, configure these in your hosting platform (e.g., Vercel, Netlify)

# Whop webhook secret - used to validate incoming webhook requests from Whop
# This should match the secret configured in your Whop app dashboard
WHOP_SECRET=dev-secret

# Whop public key - used for client-side Whop SDK initialization (if needed)
# Get this from your Whop app settings
WHOP_PUBLIC_KEY=your-whop-public-key-here

# Whop environment - controls which Whop environment to connect to
# Exposed to client-side code (NEXT_PUBLIC_ prefix)
# Values: 'development', 'production'
NEXT_PUBLIC_WHOP_ENV=development

# Alternative naming for webhook secret (currently used in codebase)
WHOP_WEBHOOK_SECRET=dev-secret

# ML Service Configuration
ML_BASE_URL=http://localhost:8000

# Next.js Configuration
# Only needed for server-side absolute URL resolution (e.g., in Server Components)
# NEXT_PUBLIC_BASE_URL=http://localhost:3000
# PORT=3000
